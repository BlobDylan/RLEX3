.venv
.conda-py313
.DS_Store
__pycache__/
*.pyc
.vscode/

# Training artifacts. Weights are large; run outputs are regenerable — the
# experiment *narrative* lives in PROJECT_ROADMAP.md, not in versioned outputs.
*.pt
*.pth
graphs/**/.mpl/
# ComplexEnv from-scratch DQN run outputs (timestamped run dirs: agent.pt [above],
# history.json, result.json, plots, rollout videos). Regenerable; keep local.
graphs/complex_dqn/
# Rollout / render videos generated under any run dir.
graphs/**/videos/
