.venv
.conda-py313
.DS_Store
__pycache__/
*.pyc
.vscode/

# Raw run outputs (weights, history, plots, videos) live under output/ — regenerable,
# not versioned. Curated report assets (graph images + rollout videos, no weights/history)
# are copied into results/ (TRACKED) via scripts/export_results.py.
# The experiment *narrative* lives in REPORT_MEMOS.md, not in versioned outputs.
*.pt
*.pth
*.log
output/
